# Applications

The repository has two application roots:

- `apps/api` for the Java 21 and Spring Boot modular monolith;
- `apps/web` for the Angular and TypeScript user interface.

The verified Spring Boot API shell is included under `apps/api`, and the verified Angular shell is included under `apps/web`. The joined runnable product flow remains planned through `NFA-007`.
