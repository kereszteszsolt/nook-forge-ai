# API application placeholder

`NFA-002` creates the Spring Boot application under this directory. The final app uses the `io.nookforge` base package, Maven Wrapper, package-by-feature boundaries, validated configuration, and no direct provider SDK use from the application or domain code.
